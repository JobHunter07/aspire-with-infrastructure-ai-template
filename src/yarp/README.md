YARP reverse-proxy (Aspire-managed)

This folder contains Aspire configuration for the standalone YARP container and route templates.
- Use Aspire's YARP integration and service-discovery to build dynamic routes.
- Example: map /api/* -> discovered backend service endpoints.
