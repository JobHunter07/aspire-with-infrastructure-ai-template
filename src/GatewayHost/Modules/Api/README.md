API Modules

Place feature folders under `Features/<FeatureName>/<Action>/<EndpointFile>.cs` following the Minimal API per-file pattern.

Register endpoints in startup so that they are discoverable by Aspire service-discovery for YARP routing.
