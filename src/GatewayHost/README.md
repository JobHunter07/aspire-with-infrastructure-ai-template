GatewayHost

GatewayHost contains the BFF and API modules. Implement API endpoints as Minimal API per-endpoint classes (one class/file per endpoint) and register service-discovery metadata so YARP can discover routes.

See `Modules/Api/Features` for sample feature layout and `Modules/Bff` for BFF responsibilities.
